import React from "react";

export const singleItems = ({item}) => {
    return(<>
            for (let key of {item}) {
                return(<>
                </>)
            }
        
    </>)
}